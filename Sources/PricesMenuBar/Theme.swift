import SwiftUI

enum Theme {
    static let background = Color(red: 0.14, green: 0.17, blue: 0.22)
    static let positive   = Color(red: 0.35, green: 0.78, blue: 0.52)   // soft mint green
    static let negative   = Color(red: 0.80, green: 0.38, blue: 0.40)   // muted dusty red
    static let warning    = Color(red: 0.95, green: 0.78, blue: 0.30)   // warm amber
}
